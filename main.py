import argparse
import classes
import races

parser = argparse.ArgumentParser(description='A command line interface tool for playing and running Dungeons and Dragons campaigns, 5th edition')

args = parser.parse_args()